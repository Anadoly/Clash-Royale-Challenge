import styled from '@emotion/styled/macro';

const CloseButton = styled.button`
  display: block;
  margin-left: auto;
  width: 35px;
  height: 35px;
  border: none;
  background: none;
  cursor: pointer;
`;

export default CloseButton;
